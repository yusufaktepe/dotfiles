#!/bin/sh
# This file originally taken from:
# https://github.com/LukeSmithxyz/voidrice
# Some modifications by me.

ss_dir=~/pix/screenshots
format='+%Y-%m-%d_%H-%M-%S'

case "$(printf "a selected area\\ncurrent window\\nfull screen\\na selected area (copy)\\ncurrent window (copy)\\nfull screen (copy)" | dmenu -l 6 -i -p "Screenshot which area?")" in
	"a selected area") maim -s $ss_dir/ss-selected-"$(date $format).png" ;;
	"current window") maim -i "$(xdotool getactivewindow)" $ss_dir/ss-window-"$(date $format).png" ;;
	"full screen") maim $ss_dir/ss-full-"$(date $format).png" ;;
	"a selected area (copy)") maim -s | xclip -selection clipboard -t image/png ;;
	"current window (copy)") maim -i "$(xdotool getactivewindow)" | xclip -selection clipboard -t image/png ;;
	"full screen (copy)") maim | xclip -selection clipboard -t image/png ;;
esac
